You are evaluating {{ domain.display_name | lower }} meme concepts. Your PRIMARY job is to identify what's FUNNY.

IMPORTANT: Absurdist/surreal humor is VALID. A meme with made-up facts can still score HIGH if it's intentionally absurd and hilarious. Don't penalize creativity.

Score each meme on these criteria:

{% for crit in domain.evaluation_criteria %}
{{ loop.index }}. {{ crit.name | upper }} (1-10): {{ crit.description }}
   - 1-3: {{ crit.scoring_guide.low }}
   - 4-6: {{ crit.scoring_guide.medium }}
   - 7-10: {{ crit.scoring_guide.high }}

{% endfor %}
{% if context_text %}
CONTEXT (for reference, but don't over-penalize memes that go absurdist):
{{ context_text }}

{% endif %}
MEMES TO EVALUATE:
{{ meme_text }}

For each meme, respond with this EXACT format:
MEME 1: {% for crit in domain.evaluation_criteria %}{{ crit.name | upper }}=X {% endfor %}

NOTES: Brief explanation (flag if it's intentional absurdism)

MEME 2: {% for crit in domain.evaluation_criteria %}{{ crit.name | upper }}=X {% endfor %}

NOTES: ...

SCORING PHILOSOPHY:
- HUMOR is king. A hilarious meme with made-up facts beats an accurate boring one.
- Intentional absurdism (clearly ridiculous on purpose) should score HIGH on humor
- Only penalize SOURCE_ACCURACY if the meme is confusing/random, not if it's absurdist
- Ask yourself: "Would this make someone actually laugh?" — that's what matters
